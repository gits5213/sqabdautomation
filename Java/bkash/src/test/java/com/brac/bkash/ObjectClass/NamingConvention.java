package com.brac.bkash.ObjectClass;

//class body
public class NamingConvention {

    //signature and method body
    //void - it will not return anything
    void sum(){

    }


    public static void main(String[] args) {
        /*
        Java naming convention is a rule to follow as you decide
        what to name your identifiers such as class, Interface, package,
        variable, constant, method, etc.

        Class:
        - It should start with the uppercase letter & following camel case.
        - It should be a noun such as Color, Button, System, Thread, etc.

        Interface:
        - It should start with the uppercase letter & following camel case.
        - It should be an adjective such as Runnable, Remote, ActionListener.

        Method
        - It should start with lowercase letter & following camel case.
        - It should be a verb such as main(), print(), println().

        Variable
        - It should start with a lowercase letter such as id, name & following camel case.
        - It should not start with the special characters like & (ampersand), $ (dollar), _ (underscore).
        - If the name contains multiple words, start it with the lowercase letter followed by an uppercase letter such as  lastName.firstName,
        Avoid using one-character variables such as x, y, z.

        Package
        - It should be a lowercase letter such as java, lang.
        - If the name contains multiple words, it should be separated by dots (.) such as java.util, java.lang.

        Constant
        - It should be in uppercase letters such as RED, YELLOW.
        - If the name contains multiple words, it should be separated by an underscore(_) such as MAX_PRIORITY.
            It may contain digits but not as the first letter.
        */


    }
}
