package com.bpo.nagad.ObjectClass;

import java.net.InterfaceAddress;

public class NamingConvention {

    /*
    Method:
    - Signature and method body
    - Void - it will not return anything
    */

    void sum()
    {

    }
    public static void main(String[] args) {

        /* Java naming convention is a rule to follow as you decide what to name
        your identifiers such as class, package, variable, constant, method, etc.
        */

        //class:
        /*
        - It should start with the uppercase letter and following camel case.
        - It should be a noun such as color, button, System, Thread, etc.
         */

        //Interface:

        /*
         - It should start with the uppercase letter and following camel case.
        - It should be a adjective such as Runnable, Remote, ActionListener.
         */

        //Method:
        /*
        - It should start with lowercase letter and following camel case.
        -it should be a verb such as main(), print(), Println().
        -if the name contains multiple words, start it with a lowercase letter
         */

        //Variable:
        /*
        - It should start with lowercase letter such as id, name and following camel case..
        -it should not start with the special characters like & (ampersand), $ (dollar), _ (underscore).
        -If the name contains multiple words, start it with the lowercase letter such as lastname.firstname,
        avoid using one-character variables such as x,y,Z.
         */




    }

}
